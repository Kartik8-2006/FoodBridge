{
  "name": "foodbridge-network",
  "version": "1.0.0",
  "private": true,
  "description": "MERN food donation management platform connecting donors, NGOs, volunteers, recipients, and admins.",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "seed": "npm run seed --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
